<%= form_for @data_class, :html => { :class => "form-horizontal data_class" } do |f| %>

    <% if @data_class.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@data_class.errors.count, "error") %> prohibited this data_class from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @data_class.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@data_class[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            data_classes_path, :class => 'btn btn-default' %>

<% end %>
